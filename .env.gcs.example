# Add these to your .env.local file for Google Cloud Storage video uploads

# Google Cloud Storage Configuration
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
GOOGLE_CLOUD_STORAGE_BUCKET=your-bucket-name

# Option 1: Path to service account key file
GOOGLE_CLOUD_KEY_FILE=./path/to/your/service-account-key.json

# Option 2: Base64 encoded service account key (alternative to key file)
# GOOGLE_CLOUD_CREDENTIALS_BASE64=your-base64-encoded-service-account-key

# Keep existing Cloudinary config for images
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret